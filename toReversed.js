// toReversed - Creates a new copy of the array and reverses it
const numbers = [1, 2, 3, 4, 5]
const reversedNums = numbers.toReversed() // creates a shallow copy and reverses it
console.log(numbers) // [1, 2, 3, 4, 5]
console.log(reversedNums) // [5, 4, 3, 2, 1]