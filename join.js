// join - Concatenates all elements into a single string separated by a separator
const words = ["Hello", "I", "am", 25, "years", "old"]
const sentence = words.join(" ") // create a string with the combined elements separated by space
console.log(sentence)